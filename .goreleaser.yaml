version: 2
before:
  hooks:
    - cargo install --locked cargo-zigbuild
builds:
  - builder: rust
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
archives:
  - formats: ["binary"]
